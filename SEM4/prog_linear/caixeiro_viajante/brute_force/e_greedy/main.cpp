#include <iostream> 
#include <vector> 
#include <chrono>
#include 

using namespace std;

int main() {
    vector<int> permut;
    for (int i = 0; i < n; i++) {
    permut.push_back(i);
    }
    do {
    // Processa permutacao
    } while (next_permutation(permut.begin(), permut.end()));
}

// ordem de entrada      
//  vertice     coordenada 
//  0           (3, 5) ...